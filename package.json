{
  "name": "@bscotch/monday",
  "version": "0.2.0",
  "description": "Simplify working with Monday.com boards for server-side actions in Node.js.",
  "main": "./build/index.js",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "test": "npm run build && npx mocha ./build/test/index.js",
    "build": "rm -rf build && npx tsc",
    "prepare": "npm run build",
    "preversion": "git checkout develop && git pull && npm test",
    "postversion": "git push && git checkout master && git merge develop && git push && npm publish && git checkout develop"
  },
  "keywords": [
    "monday",
    "monday.com",
    "api"
  ],
  "dependencies": {
    "cross-fetch": "^3.0.5"
  },
  "devDependencies": {
    "@types/chai": "^4.2.12",
    "@types/mocha": "^8.0.1",
    "@types/node": "^14.0.27",
    "@typescript-eslint/eslint-plugin": "^3.8.0",
    "@typescript-eslint/parser": "^3.8.0",
    "chai": "^4.2.0",
    "dotenv": "^8.2.0",
    "eslint": "^7.6.0",
    "mocha": "^8.1.0",
    "typescript": "^3.9.7"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bscotch/monday.git"
  },
  "author": "Adam Coster",
  "license": "MIT"
}
